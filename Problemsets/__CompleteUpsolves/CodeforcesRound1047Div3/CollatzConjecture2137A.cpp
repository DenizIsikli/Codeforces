#include <bits/stdc++.h>

#define int long long
#define endl '\n'

using namespace std;

int k,x;

void solve() {
    cin>>k>>x;
    cout<<(x<<k)<<endl;
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    std::cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}

